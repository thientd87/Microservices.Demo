version: '3.4'

services:
    catalogdb:
        image: mongo

    catalog.api:
        image: ${DOCKER_REGISTRY-}catalog
        build:
            context: .
            dockerfile: src/Catalog/Catalog.API/Dockerfile
